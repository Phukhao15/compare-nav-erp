# ระบบเปรียบเทียบข้อมูล NAV vs ERP (NAV-ERP Comparison Tool)

โปรเจกต์นี้เป็น Web Application สำหรับเปรียบเทียบและตรวจสอบความถูกต้องของข้อมูลการขาย (Sales Order) ระหว่างสองระบบคือ **Microsoft Dynamics NAV** และ **ERP** เพื่อช่วยให้ฝ่ายบัญชีหรือผู้ดูแลระบบสามารถตรวจสอบยอดขายและสถานะเอกสารได้ง่ายขึ้น

## 📋 ความสามารถหลัก (Features)

ระบบประกอบด้วย 4 หน้าหลักสำหรับการตรวจสอบข้อมูล:

1.  **เปรียบเทียบรายเดือน (Monthly Comparison)**
    *   ดูภาพรวมยอดขายเปรียบเทียบรายเดือน
    *   แสดงยอดรวมของทั้งสองระบบและผลต่าง (Diff)

2.  **เปรียบเทียบตาม Sales (Salesperson Comparison)**
    *   ดูยอดขายแยกตามพนักงานขาย (Salesperson)
    *   เปรียบเทียบจำนวนเอกสารและยอดเงินระหว่าง NAV และ ERP
    *   แสดงสถานะเอกสารใน ERP (Draft/Submitted)

3.  **เปรียบเทียบตาม Sales V2 (Amount > 0)**
    *   เหมือนหน้าเปรียบเทียบตาม Sales แต่จะกรองเฉพาะรายการที่มี **Amount > 0**
    *   ช่วยกรองข้อมูลขยะหรือเอกสารที่ไม่มีมูลค่าออกไป

4.  **เช็คยอดชนใบ (Match List / Collision Check)**
    *   ตรวจสอบรายการต่อรายการ (Transaction level)
    *   จับคู่เอกสารระหว่าง NAV และ ERP เพื่อดูว่ามีใบไหนตกหล่นหรือยอดไม่ตรงกัน

## 🛠️ การติดตั้งและการตั้งค่า (Installation)

### ความต้องการของระบบ (Requirements)
*   Web Server (Apache/Nginx/IIS)
*   PHP 7.4 หรือสูงกว่า
*   PDO Extension สำหรับเชื่อมต่อฐานข้อมูล

### การตั้งค่าฐานข้อมูล (Database Configuration)
ระบบนี้ต้องการไฟล์ `dbconnect.inc.php` ซึ่งควรวางไว้นอกโฟลเดอร์เว็บ (Parent Directory) เพื่อความปลอดภัย

โครงสร้างไฟล์ `../dbconnect.inc.php`:

```php
<?php
// ตัวอย่างการตั้งค่า
$host = 'localhost';
$db   = 'your_database';
$user = 'your_username';
$pass = 'your_password';

// การเชื่อมต่อ PDO สำหรับ NAV และ ERP
// $pdoNav = ...
// $pdoErp = ...

// การตั้งค่าชื่อตาราง (Table Names)
$NAV_T = [
    'SO' => 'Sales Header',      // ตาราง Sales Order
    'SP' => 'Salesperson/Purchaser', // ตารางพนักงานขาย
    'SL' => 'Sales Line',        // ตารางรายการสินค้า
    // ... อื่นๆ
];
?>
```

## 🚀 วิธีการใช้งาน (Usage)

1.  เปิดหน้าแรก `index.php` ระบบจะแสดงหน้า **เปรียบเทียบรายเดือน** เป็นค่าเริ่มต้น
2.  ใช้เมนูแท็บด้านบนเพื่อเปลี่ยนโหมดการตรวจสอบ:
    *   *เปรียบเทียบรายเดือน*
    *   *เปรียบเทียบตาม Sales*
    *   *เปรียบเทียบตาม SalesV2*
    *   *เช็คยอดชนใบ*
3.  ในแต่ละหน้าจะมีตัวกรอง (Filter) เช่น **Prefix เดือน** (เช่น `2509`, `2510`) ให้ระบุปีและเดือนที่ต้องการตรวจสอบ แล้วกดปุ่ม **Filter**

## 📂 โครงสร้างไฟล์ (File Structure)

*   `index.php`: ไฟล์หลักสำหรับควบคุมการทำงาน (Router)
*   `common_header.php`: ส่วนหัวของหน้าเว็บและเมนู
*   `common_footer.php`: ส่วนท้ายของหน้าเว็บ
*   `page_compare_*.php`: ไฟล์ Logic สำหรับการแสดงผลแต่ละหน้า
*   `bootstrap/`: โฟลเดอร์เก็บไฟล์ CSS ของ Bootstrap Framework

## 🎨 ฟีเจอร์ UI/UX

### การออกแบบที่ทันสมัย
*   **Dynamic Theme Colors**: สีของระบบจะเปลี่ยนไปตามเดือนที่เลือก (12 สีประจำเดือน)
*   **Responsive Design**: รองรับการแสดงผลบนมือถือและแท็บเล็ต
*   **Loading State**: แสดงสถานะกำลังโหลดข้อมูลเพื่อประสบการณ์ที่ดีขึ้น
*   **Copy to Clipboard**: ปุ่ม Copy Table สำหรับคัดลอกข้อมูลไปวางใน Excel ได้ทันที

### ฟอนต์และสไตล์
*   ใช้ฟอนต์ **Prompt** จาก Google Fonts เพื่อความสวยงามและอ่านง่าย
*   Card Design พร้อม Shadow Effects
*   Hover Effects บนตารางเพื่อการอ่านที่ง่ายขึ้น



```



